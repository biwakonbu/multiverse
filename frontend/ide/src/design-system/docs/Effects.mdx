import { Meta, Unstyled } from "@storybook/blocks";

<Meta title="Design System/Tokens/Effects" />

# Effects

Multiverse uses "Phantom Glass" and "Aurora Glow" aesthetic principles.
Effects are critical for conveying depth, state, and focus in a dark environment.

## Shadows

Used for elevation and depth.

<Unstyled>
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "repeat(auto-fill, minmax(200px, 1fr))",
      gap: "32px",
      padding: "32px 0",
    }}
  >
    <div
      style={{
        height: "100px",
        background: "#2e3440",
        borderRadius: "8px",
        boxShadow:
          "0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#eceff4",
      }}
    >
      Base (No specific token)
    </div>
    <div
      style={{
        height: "100px",
        background: "#3b4252",
        borderRadius: "8px",
        boxShadow: "var(--mv-shadow-lg)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#eceff4",
      }}
    >
      Large (--mv-shadow-lg)
    </div>
    <div
      style={{
        height: "100px",
        background: "#242832",
        borderRadius: "8px",
        boxShadow: "var(--mv-shadow-inset-sm)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#eceff4",
      }}
    >
      Inset Small (--mv-shadow-inset-sm)
    </div>
  </div>
</Unstyled>

## Glows

Used for status and interactivity.

<Unstyled>
  <div
    style={{
      display: "grid",
      gridTemplateColumns: "repeat(auto-fill, minmax(200px, 1fr))",
      gap: "32px",
      padding: "32px 0",
    }}
  >
    <div
      style={{
        height: "100px",
        background: "#3b4252",
        borderRadius: "8px",
        boxShadow: "0 0 8px rgba(136, 192, 208, 0.4)",
        border: "1px solid #88c0d0",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#88c0d0",
      }}
    >
      Focus / Selected
    </div>
    <div
      style={{
        height: "100px",
        background: "#1e2d26",
        borderRadius: "8px",
        boxShadow: "0 0 8px rgba(143, 191, 159, 0.35)",
        border: "1px solid #8fbcbb",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#a3be8c",
      }}
    >
      Running / Active
    </div>
    <div
      style={{
        height: "100px",
        background: "#2d1f22",
        borderRadius: "8px",
        boxShadow: "0 0 8px rgba(191, 97, 106, 0.3)",
        border: "1px solid #bf616a",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#bf616a",
      }}
    >
      Error / Failed
    </div>
  </div>
</Unstyled>

## Animations

<Unstyled>
  <div style={{ display: "flex", flexDirection: "column", gap: "24px" }}>
    <div style={{ display: "flex", alignItems: "center", gap: "24px" }}>
      <div style={{ width: "120px", color: "#eceff4" }}>Pulse</div>
      <div
        style={{
          width: "40px",
          height: "40px",
          borderRadius: "50%",
          background: "#a3be8c",
          boxShadow: "0 0 8px rgba(143, 191, 159, 0.35)",
          animation: "pulse 2s infinite",
        }}
      />
      <style>{`
        @keyframes pulse {
          0% { box-shadow: 0 0 0 0 rgba(143, 191, 159, 0.4); }
          70% { box-shadow: 0 0 0 10px rgba(143, 191, 159, 0); }
          100% { box-shadow: 0 0 0 0 rgba(143, 191, 159, 0); }
        }
      `}</style>
      <div style={{ fontSize: "12px", color: "#81a1c1" }}>
        Used for RUNNING status
      </div>
    </div>
  </div>
</Unstyled>
